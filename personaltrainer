<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Free Personal Training Consultation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    /* RESET */
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
      background: linear-gradient(135deg, #111, #222);
      color: #fff;
      line-height: 1.6;
    }

    .container {
      max-width: 1100px;
      margin: auto;
      padding: 3rem 1.2rem;
    }

    /* HERO */
    .hero {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 2rem;
      align-items: center;
      min-height: 100vh;
    }

    .hero h1 {
      font-size: 2.6rem;
      margin-bottom: 1rem;
    }

    .hero p {
      color: #ccc;
      margin-bottom: 1.5rem;
    }

    .benefits li {
      list-style: none;
      margin-bottom: 0.6rem;
    }

    .benefits li::before {
      content: "✔ ";
      color: #00e676;
    }

    /* FORM CARD */
    .form-card {
      background: #fff;
      color: #111;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    .form-card h2 {
      margin-bottom: 1rem;
      font-size: 1.6rem;
    }

    .form-card p {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 1.2rem;
    }

    .form-card input,
    .form-card select {
      width: 100%;
      padding: 0.8rem;
      margin-bottom: 0.8rem;
      border-radius: 0.4rem;
      border: 1px solid #ddd;
      font-size: 0.95rem;
    }

    .form-card button {
      width: 100%;
      padding: 0.9rem;
      background: #00e676;
      color: #111;
      font-weight: 600;
      border: none;
      border-radius: 999px;
      cursor: pointer;
      font-size: 1rem;
      transition: transform 0.2s ease;
    }

    .form-card button:hover {
      transform: translateY(-2px);
    }

    #status {
      margin-top: 0.8rem;
      font-size: 0.9rem;
    }

    /* SOCIAL PROOF */
    .trust {
      margin-top: 2rem;
      font-size: 0.85rem;
      color: #aaa;
    }

    /* RESPONSIVE */
    @media (max-width: 900px) {
      .hero {
        grid-template-columns: 1fr;
        text-align: center;
      }
    }
  </style>
</head>

<body>

<div class="container">
  <section class="hero">

    <!-- LEFT CONTENT -->
    <div>
      <h1>Get Your Free Personal Training Consultation</h1>
      <p>
        Ready to feel stronger, leaner, and more confident?
        Book a free consultation and get a personalised fitness plan.
      </p>

      <ul class="benefits">
        <li>Custom fitness assessment</li>
        <li>Clear plan for your goals</li>
        <li>No pressure, no obligation</li>
        <li>Limited weekly spots</li>
      </ul>

      <div class="trust">
        Trusted by clients looking to lose weight, build muscle, and feel better.
      </div>
    </div>

    <!-- FORM -->
    <div class="form-card">
      <h2>Claim Your Free Session</h2>
      <p>Fill in your details and I’ll be in touch shortly.</p>

      <form id="leadForm">
        <input type="text" name="name" placeholder="Full Name" required>
        <input type="email" name="email" placeholder="Email Address" required>
        <input type="tel" name="phone" placeholder="Phone Number" required>

        <select name="goal" required>
          <option value="">Your Primary Goal</option>
          <option>Weight loss</option>
          <option>Muscle gain</option>
          <option>General fitness</option>
        </select>

        <button type="submit">Start My Transformation</button>
      </form>

      <p id="status"></p>
    </div>

  </section>
</div>

<script>
const form = document.getElementById("leadForm");
const status = document.getElementById("status");

form.addEventListener("submit", function(e) {
  e.preventDefault();

  const data = {
    name: form.name.value,
    email: form.email.value,
    phone: form.phone.value,
    goal: form.goal.value
  };


  fetch("https://script.google.com/macros/s/AKfycbwvyn1jSco-g8YaZ92tSz1TYRK5UFjsVrt8bP2jurZyaCZmcRiWqTJtD3TyPjq9q6j3Jw/exec", 
    { mode: "no-cors" },
   {
     method: "POST",
     body: JSON.stringify(data),
     headers: { "Content-Type": "application/json" }
  }
  )
  .then(res => res.json())
  .then(() => {
    status.textContent = "Thanks! Check your email shortly 💪";
    form.reset();
  })
  .catch(() => {
    status.textContent = "Something went wrong. Please try again.";
  });
});
</script>

</body>
</html>
